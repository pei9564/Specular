// @provisional
// Domain: auth
// Generated by: /speckit.specify (001-auth-InitialAdmin)
// Date: 2026-02-12
// Status: Provisional — must be ratified during /speckit.plan

Table users {
  id uuid [pk, not null, unique, note: 'Primary key']
  username varchar(50) [not null, unique, note: 'Login identifier']
  email varchar(255) [not null, unique, note: 'Contact email']
  password_hash varchar(255) [not null, note: 'One-way hashed password; plaintext never stored']
  role user_role [not null, default: 'member', note: 'Authorization level']
  is_active boolean [not null, default: true, note: 'Account activation status']
  created_at timestamptz [not null, default: `now()`, note: 'UTC timestamp of account creation']
  updated_at timestamptz [not null, default: `now()`, note: 'UTC timestamp of last modification']
}

Enum user_role {
  super_admin [note: 'Full system privileges']
  admin [note: 'Administrative privileges']
  member [note: 'Standard user']
}
