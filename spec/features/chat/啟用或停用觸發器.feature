Feature: 啟用或停用觸發器
  # 控制 Agent 觸發器的活躍狀態

  Rule: 啟用或停用觸發器 的核心邏輯
    # 定義 啟用或停用觸發器 的相關規則與行為

    Example: 停用觸發器
      When 用戶停用某個排程觸發器
      Then 即使時間到達，該觸發器也不應執行

    Example: 啟用觸發器
      When 用戶重新啟用觸發器
      Then 該觸發器應恢復正常運作
